-- Services
local TweenService = game:GetService("TweenService")

-- GUI Elements
local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local Emoji1 = Instance.new("TextLabel")
local Emoji2 = Instance.new("TextLabel")
local Emoji3 = Instance.new("TextLabel")
local Logo = Instance.new("ImageLabel")
local BrandLabel = Instance.new("TextLabel")
local MessageLabel = Instance.new("TextLabel")

-- Setup ScreenGui
ScreenGui.Name = "WarningMessage"
ScreenGui.Parent = game.CoreGui

-- Frame
Frame.Name = "MainFrame"
Frame.Parent = ScreenGui
Frame.AnchorPoint = Vector2.new(0.5, 0.5)
Frame.Position = UDim2.new(0.5, 0, 0.5, 0)
Frame.Size = UDim2.new(0, 460, 0, 320)
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true

-- Function to create pulse animation
local function createPulse(label)
	local up = TweenService:Create(label, TweenInfo.new(0.5, Enum.EasingStyle.Sine), {TextSize = 34})
	local down = TweenService:Create(label, TweenInfo.new(0.5, Enum.EasingStyle.Sine), {TextSize = 28})
	task.spawn(function()
		while label and label.Parent do
			up:Play()
			up.Completed:Wait()
			down:Play()
			down.Completed:Wait()
		end
	end)
end

-- Emoji Labels 🐶 🦖 🦝
local emojis = {"🐶", "🦖", "🦝"}
local positions = {-85, -25, 35}
local emojiLabels = {Emoji1, Emoji2, Emoji3}

for i, label in ipairs(emojiLabels) do
	label.Parent = Frame
	label.Size = UDim2.new(0, 50, 0, 30)
	label.Position = UDim2.new(0.5, positions[i], 0, 10)
	label.BackgroundTransparency = 1
	label.Font = Enum.Font.SourceSansBold
	label.Text = emojis[i]
	label.TextColor3 = Color3.fromRGB(255, 255, 0)
	label.TextSize = 28
	label.TextXAlignment = Enum.TextXAlignment.Center
	label.TextYAlignment = Enum.TextYAlignment.Center
	createPulse(label)
end

-- Avengers Logo (centered)
Logo.Name = "Logo"
Logo.Parent = Frame
Logo.BackgroundTransparency = 1
Logo.Position = UDim2.new(0.5, -30, 0, 50)
Logo.Size = UDim2.new(0, 60, 0, 60)
Logo.Image = "rbxassetid://7268802369" -- Marvel Avengers logo

-- Centered RD SCRIPTS text under logo
BrandLabel.Name = "Brand"
BrandLabel.Parent = Frame
BrandLabel.BackgroundTransparency = 1
BrandLabel.Position = UDim2.new(0.5, -100, 0, 115)
BrandLabel.Size = UDim2.new(0, 200, 0, 30)
BrandLabel.Font = Enum.Font.SourceSansBold
BrandLabel.Text = "RD SCRIPTS"
BrandLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
BrandLabel.TextSize = 28
BrandLabel.TextXAlignment = Enum.TextXAlignment.Center

-- Message Text
MessageLabel.Name = "Message"
MessageLabel.Parent = Frame
MessageLabel.BackgroundTransparency = 1
MessageLabel.Position = UDim2.new(0.5, -200, 0, 160)
MessageLabel.Size = UDim2.new(0, 400, 0, 120)
MessageLabel.Font = Enum.Font.SourceSansBold
MessageLabel.Text = "Currently not working on Delta.\nUse KRNL, or turn off your Anti Scam button on Delta."
MessageLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
MessageLabel.TextSize = 18
MessageLabel.TextWrapped = true
MessageLabel.TextXAlignment = Enum.TextXAlignment.Center
MessageLabel.TextYAlignment = Enum.TextYAlignment.Top
