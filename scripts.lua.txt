-- GUI with animated smiley face, logo, and message
local TweenService = game:GetService("TweenService")

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local Logo = Instance.new("ImageLabel")
local SmileyLabel = Instance.new("TextLabel")
local MessageLabel = Instance.new("TextLabel")

-- Parent the GUI
ScreenGui.Name = "WarningMessage"
ScreenGui.Parent = game.CoreGui

-- Main Frame
Frame.Name = "MainFrame"
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.BorderSizePixel = 0
Frame.Position = UDim2.new(0.5, -200, 0.5, -120)
Frame.Size = UDim2.new(0, 400, 0, 240)
Frame.AnchorPoint = Vector2.new(0.5, 0.5)
Frame.Active = true
Frame.Draggable = true

-- Smiley Face Label
SmileyLabel.Name = "Smiley"
SmileyLabel.Parent = Frame
SmileyLabel.BackgroundTransparency = 1
SmileyLabel.Position = UDim2.new(0.5, -25, 0, 5)
SmileyLabel.Size = UDim2.new(0, 50, 0, 30)
SmileyLabel.Font = Enum.Font.SourceSansBold
SmileyLabel.Text = "😊"
SmileyLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
SmileyLabel.TextSize = 28
SmileyLabel.TextXAlignment = Enum.TextXAlignment.Center
SmileyLabel.TextYAlignment = Enum.TextYAlignment.Center

-- Animate the Smiley Face (pulse)
local function pulseEmoji()
	local scaleUp = TweenService:Create(SmileyLabel, TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {TextSize = 34})
	local scaleDown = TweenService:Create(SmileyLabel, TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {TextSize = 28})

	while SmileyLabel and SmileyLabel.Parent do
		scaleUp:Play()
		scaleUp.Completed:Wait()
		scaleDown:Play()
		scaleDown.Completed:Wait()
	end
end

task.spawn(pulseEmoji)

-- Logo Image
Logo.Name = "Logo"
Logo.Parent = Frame
Logo.BackgroundTransparency = 1
Logo.Position = UDim2.new(0.5, -50, 0, 40)
Logo.Size = UDim2.new(0, 100, 0, 100)
Logo.Image = "rbxassetid://13413215459" -- Replace with your custom logo ID if you have one

-- Message Label
MessageLabel.Name = "Message"
MessageLabel.Parent = Frame
MessageLabel.BackgroundTransparency = 1
MessageLabel.Position = UDim2.new(0.5, -180, 0, 150)
MessageLabel.Size = UDim2.new(0, 360, 0, 70)
MessageLabel.Font = Enum.Font.SourceSansBold
MessageLabel.Text = "Currently not working on Delta.\nUse KRNL, or turn off your Anti Scam button on Delta."
MessageLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
MessageLabel.TextSize = 18
MessageLabel.TextWrapped = true
MessageLabel.TextXAlignment = Enum.TextXAlignment.Center
MessageLabel.TextYAlignment = Enum.TextYAlignment.Top
